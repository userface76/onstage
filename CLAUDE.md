# 온스테이지 ONSTAGE

가수 · 밴드 · 트롯 · 배우 · 마술사에게 **홈페이지를 만들어 주고 목록에 올려 주는** 서비스.
사이트는 JSON 한 벌에서 생성한다. 손으로 HTML을 고치지 않는다.

## 축이 두 개다 — 이것이 이 플랫폼의 전부

```
category (직군)  →  어떤 블록이 들어가나
type     (유형)  →  어떤 순서로 배치하나
```

| category | 고유 블록 |
|---|---|
| `magician` | 레퍼토리 (액트 · 소요시간 · 무대조건) |
| `singer` | 대표곡 · 음원 링크 + 행사 구성 |
| `actor` | 필모그래피 (연도 · 작품 · 역할 · 매체) |
| `band` | 멤버 + 수록곡 |

| type | 첫 화면 | 어울리는 경우 |
|---|---|---|
| `A` | 대표 이미지 · 이름 | 일정이 드물고 인상만 남기면 될 때 |
| `B` | 다음 일정 | 정기 공연이 있을 때 |
| `C` | 섭외 버튼 + 사실 4칸 | 섭외가 곧 수입일 때 |

**공통 블록**(프로필 · 갤러리 · 일정 · 이력 · 섭외)은 모든 조합에 들어간다.
직군이 늘면 `src/render.mjs`의 `CATEGORY_BLOCKS`에 블록 하나만 추가한다.

## 아티스트 추가하는 법

1. `data/artists/<slug>.json` 을 만든다 (`hwanghwi.json` 이 본보기)
2. 사진을 `public/artists/<slug>/` 에 넣는다 — JSON의 사진 경로는 파일명만 적으면 된다
3. `npm run build`

`slug` · `name` · `category` · `type` 이 없거나 이상하면 빌드가 멈춘다.

## 빈 값 규칙 — 중요

**값이 비면 그 블록은 나오지 않는다.** 다만 `"draft": true` 이면 「채울 자리」 안내가 대신 나온다.

- 시안 단계 → `draft: true`. 무엇을 더 받아야 하는지 화면이 스스로 말한다
- 오픈 → `draft: false`. 빈 것은 조용히 사라진다

이 규칙 덕분에 자료가 덜 모여도 사이트를 먼저 만들어 보여줄 수 있다.

## 절대 지키는 것

1. **실존 인물의 이력을 지어내지 않는다.** 경력 · 수상 · 출연작은 본인이 준 자료에서만 옮긴다.
   모르면 비워 두고 `draft: true` 로 둔다.
2. **실명과 사진이 들어간 페이지는 본인 동의 후에 공개한다.** 예시는 `홍길동`을 쓰고 `"sample": true` 로 표시한다.
3. **약속한 것은 지킨다.** 랜딩에 이렇게 적혀 있다 — 3~5일 납기(베이직), 수정 2회 무상,
   도메인 본인 명의, 직접 수정 가능. 기능을 자르면 이 문구부터 고친다.
4. **단가 공개는 본인 선택.** 대신 소요 시간과 조건은 적게 한다 — 헛걸음 문의가 크게 준다.
5. **모바일 우선.** "담당자 열에 아홉은 휴대폰으로 봅니다"라고 랜딩에 써 두었다.

## 폴더

```
data/site.json          브랜드 이름 · 주소
data/artists/*.json     아티스트 한 명 = 파일 하나
public/artists/<slug>/  사진
src/theme.mjs           CSS. 어두운 무대 톤 하나. 아티스트마다 강조색(accent)만 다름
src/render.mjs          아티스트 → HTML. 직군 블록 + 유형 배치
src/pages.mjs           랜딩 · 목록
build.mjs               전부 묶어 dist/ 로
dist/                   빌드 결과. git에 올리지 않는다 (Cloudflare가 직접 만든다)
```

## 배포 — Cloudflare Pages

GitHub에 push하면 Cloudflare가 알아서 빌드해 올린다.

- Build command : `npm run build`
- Output directory : `dist`

주소 구조는 `onstage.pages.dev/<slug>/`. 나중에 아티스트 개인 도메인을 붙일 때는
Cloudflare Pages의 Custom domains 에서 해당 경로로 연결한다.

## 남은 것

- [ ] 문의폼 실제 전송 (지금은 화면만) — Cloudflare Pages Functions + 메일/문자 발송
- [ ] 아티스트가 직접 고치는 편집 화면 — 랜딩에서 약속한 기능이다
- [ ] 목록 필터 (직군 · 지역)
- [ ] 밝은 테마 — 배우 쪽에서 요구가 나올 수 있다
- [ ] 요금 실제 금액 확정 (지금은 「견적 문의」로만)
